<?xml version="1.0" encoding="utf-8"?>
<manifest package="de.aw.awlib"
          xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.INTERACT_ACROSS_USERS_FULL"/>
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/AWLibTheme">

        <activity android:name=".activities.AWLibActivityDebug">
        </activity>
        <activity android:name=".activities.AWLibWebViewActivity">
        </activity>
        <activity android:name=".activities.AWLibPreferenceActivity">
        </activity>

        <service
            android:name=".events.AWLibEventService"
            android:label="@string/awlib_eventservice"
            />

    </application>

</manifest>